extends layout

block content

    // Main content
    .container
        .columns
            .column.is-one-third
            .column.is-one-third
                form(method='POST' action='')
                    .field
                        label.label Username
                        .control.has-icons-left.has-icons-right
                            input.input#username(type='text' placeholder='username' name='username' required value=(undefined===user ? '' : user.username))
                            span.icon.is-small.is-left
                                i.fas.fa-user
                    .field
                        label.label Password
                        .control
                            // TODO type='password'
                            input.input#password(type='text' name='password' required)
                    .field
                        .control
                            // type='button' value='logIn'
                            a.button.is-primary(type='submit')
                                | Log in
                    br
                    a(href='/users/reset') Forgot your password?
                    br
            .column.is-one-third
    br
    if errors
        ul
            for error in errors
                li!= error.msg